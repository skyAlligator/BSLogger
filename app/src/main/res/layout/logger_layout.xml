<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/grayimage"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ScrollView
        android:id="@+id/log_scrollView"
        style="@style/scrollbar_shape_style"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/logger_layout_button">

        <TextView
            android:id="@+id/main_logTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="#000000"
            android:textStyle="bold" />
    </ScrollView>

    <Button
        android:id="@+id/logger_layout_button"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:text="@string/close_log"
        android:textColor="#230606" />

    <!--<SeekBar
        android:id="@+id/alpha_seekBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/log_scrollView"
        android:layout_alignRight="@+id/log_scrollView"
        android:layout_alignTop="@+id/logger_layout_button"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@+id/logger_layout_button"
        android:layout_toRightOf="@+id/logger_layout_button"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:progress="100" />-->

</RelativeLayout>
